{
  "name": "arbitrage-playbook",
  "private": true,
  "packageManager": "pnpm@11.5.0",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "format": "vp fmt . --write",
    "format:check": "vp fmt . --check",
    "lifi": "pnpm --filter @arbitrage-playbook/lifi",
    "lma": "pnpm --filter @arbitrage-playbook/layerzero-multichain-arbitrage"
  },
  "devDependencies": {
    "vite-plus": "0.2.6"
  }
}
